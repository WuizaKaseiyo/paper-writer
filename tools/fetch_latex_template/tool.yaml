id: fetch_latex_template
name: fetch_latex_template
description: Copy a vendored ICLR/NeurIPS venue template (bundled in this tool's templates/ dir) into a destination directory so the LLM can overwrite main.tex. Runs fully offline — no network or git.
type: langchain_module
added_by: talent:paper-writer
source_talent: paper-writer
